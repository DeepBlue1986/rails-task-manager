<h1>To Do Manager</h1>
<!-- <p>Find me in app/views/tasks/index.html.erb</p>
 -->

<% @tasks.each do |task| %>
<%= link_to task.name, task_path(task) %> <%= task.name %> - <%= task.details %> details <%= link_to 'Delete!', task_path(task), method: :delete , data: {confirm: 'Are you sure??'}%>
<% end %>
